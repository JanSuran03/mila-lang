program sortSelect;

var i, j, i_min : integer;
var arr : array [0 .. 9] of integer;

begin
    for i := 0 to 10 do
        readln(arr[i]);

    for i := 0 to 10 do begin # select from unsorted
        i_min := i;
        for j := i + 1 to 10 do
            if (arr[j] < arr[i_min]) then
                i_min := j;
        swap(arr[i], arr[i_min]);
    end;

    for i := 0 to 10 do
        writeln(arr[i]);
end.
