program arrayMax;

var I, MAX : integer;
var X : array [0 .. 5] of integer;
begin
  X[0] := 11;
  X[1] := 66;
  X[2] := 128;
  X[3] := 49;
  X[4] := 133;
  X[5] := 46;

  for I := 0 to 6 do begin
    writeln(X[I]);
  end;
  MAX := X[0];
  for I := 1 to 6 do begin
    if(MAX < X[I]) then MAX := X[I];
  end;
  writeln(MAX);
end.
